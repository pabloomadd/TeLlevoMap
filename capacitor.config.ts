import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pabloomadd.tellevoapp',
  appName: 'TeLlevoApp',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
